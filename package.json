{
  "name": "crm-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "build:render-api": "npm ci && node ./node_modules/prisma/build/index.js migrate deploy --schema ./server/prisma/schema.prisma && node ./node_modules/prisma/build/index.js generate --schema ./server/prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
